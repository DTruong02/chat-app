{
  "installCommand": "npm install && npm --prefix public install",
  "buildCommand": "npm --prefix public run build",
  "outputDirectory": "public/build",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}